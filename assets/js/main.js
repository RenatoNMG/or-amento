const header=document.querySelector(".header");const menuToggle=document.querySelector(".menu-toggle");const nav=document.querySelector(".nav-links");const year=document.querySelector("#year");if(year)year.textContent=new Date().getFullYear();window.addEventListener("scroll",()=>{header.classList.toggle("scrolled",window.scrollY>20)},{passive:true});menuToggle?.addEventListener("click",()=>{const open=nav.classList.toggle("open");menuToggle.setAttribute("aria-expanded",String(open))});nav?.querySelectorAll("a").forEach(link=>link.addEventListener("click",()=>{nav.classList.remove("open");menuToggle?.setAttribute("aria-expanded","false")}));const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("visible");observer.unobserve(entry.target)}})},{threshold:.12});document.querySelectorAll(".reveal").forEach(el=>observer.observe(el));document.querySelector("#contact-form")?.addEventListener("submit",event=>{event.preventDefault();const note=document.querySelector("#form-note");note.textContent="Mensagem registrada nesta versão inicial. O canal de envio será conectado na próxima etapa.";note.style.color="#72f1ae";event.target.reset()});
const tilt=document.querySelector(".code-window");if(tilt&&window.matchMedia("(pointer:fine)").matches){const visual=document.querySelector(".hero-visual");visual.addEventListener("mousemove",e=>{const box=visual.getBoundingClientRect(),x=(e.clientX-box.left)/box.width-.5,y=(e.clientY-box.top)/box.height-.5;tilt.style.transform="rotateX("+(-y*5)+"deg) rotateY("+(x*7)+"deg) rotateZ(1deg)"});visual.addEventListener("mouseleave",()=>{tilt.style.transform="rotateX(0) rotateY(0) rotateZ(1.5deg)"})}